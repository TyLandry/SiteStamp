import LogoutButton from "../components/LogoutButton";

export default function AdminDashboard() {
  return (
    <div style={{ padding: 40 }}>
      <h1>Admin Dashboard</h1>
      <p>Employer view — placeholder</p>
      <LogoutButton />
    </div>
  );
}